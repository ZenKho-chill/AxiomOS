name: Release Artifacts

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    name: Create Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Skeleton: Các bước release sẽ được bổ sung chi tiết sau
      - name: Release placeholder
        run: echo "Release placeholder"
