[workspace]
members = [
    "kernel",
    "userspace/init",
    "userspace/shell",
    "userspace/libc",
    "tools/image-builder",
    "tools/xtask",
]
resolver = "2"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
